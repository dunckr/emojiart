<div class="panel-heading">
  <h4 class="panel-title">
    <a data-toggle="collapse" data-parent="#accordion" href="#{{title}}">
        <h4 class="group-heading">{{title}}</h4>
        <span class="group-icon glyphicon glyphicon-resize-vertical pull-right"></span>
    </a>
  </h4>
</div>
<div id="{{title}}" class="panel-collapse collapse in">
  <div id="group" class="panel-body">
    {{#each collection}}
        <span>{{value}}</span>
    {{/each}}
  </div>
</div>
